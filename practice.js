// create a function that takes two parameters, one for an action, one for a value
// the function logs to the console the action and the value

// create a variable named action and assign a action string to it
// create a variable named value and assign a "value" string for it
// pass action and value to the function;

const action = "this is my action string";
const value = "this is my value string";
const myFunction = function(action, value) {
    // [whatever action you passed]
    // [whatever value you passed]
   console.log(action);
   console.log(value);
}

myFunction(action, value);








